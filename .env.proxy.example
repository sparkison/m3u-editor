# M3U Editor with External Proxy - Environment Variables
# Copy this file to .env and update the values

# =============================================================================
# REQUIRED: Generate secure tokens before deploying
# =============================================================================

# Generate with: openssl rand -hex 32
M3U_PROXY_TOKEN=changeme-generate-secure-token

# Generate with: openssl rand -base64 32
PG_PASSWORD=changeme-generate-secure-password

# =============================================================================
# Application Configuration
# =============================================================================

# Your application URL (domain, IP, or localhost)
# Examples:
#   - http://localhost
#   - http://192.168.1.100
#   - https://m3u.yourdomain.com
APP_URL=http://localhost

# =============================================================================
# PostgreSQL Database Configuration
# =============================================================================

PG_DATABASE=m3ue
PG_USER=m3ue
# PG_PASSWORD is set above (required)
PG_PORT=5432

# =============================================================================
# Optional: Redis Configuration
# =============================================================================

# Redis is configured in docker-compose.yml by default
# Uncomment and modify if you need custom Redis settings
# REDIS_URL=redis://redis:6379/0
# REDIS_PASSWORD=your-redis-password

# =============================================================================
# Optional: M3U Proxy Advanced Configuration
# =============================================================================

# Log level for m3u-proxy (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# Redis pooling configuration
# ENABLE_REDIS_POOLING=true
# REDIS_POOL_MAX_CONNECTIONS=50

# Stream management
# STREAM_TIMEOUT=300
# CLEANUP_INTERVAL=60

# =============================================================================
# Deployment Notes
# =============================================================================

# 1. Generate secure tokens:
#    M3U_PROXY_TOKEN=$(openssl rand -hex 32)
#    PG_PASSWORD=$(openssl rand -base64 32)
#
# 2. Update APP_URL to your actual domain or IP
#
# 3. Start services:
#    docker-compose up -d
#
# 4. Verify deployment:
#    docker-compose ps
#    docker exec -it m3u-editor php artisan m3u-proxy:status
#
# 5. Access application:
#    Open browser to APP_URL (default: http://localhost:36400)
