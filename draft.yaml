models:
  Playlist:
    name: string
    url: string
    prefix: string nullable
    channels: unsignedInteger default:0
    synced: datetime nullable
    errors: longText nullable
    relationships:
      hasMany: Channel, Group

  Group:
    name: string
    playlist_id: unsignedInteger
    relationships:
      belongsTo: Playlist

  Channel:
    name: string
    enabled: boolean default:true
    channel: unsignedInteger nullable
    url: string
    logo: string
    group: string
    tvgid: string
    lang: string
    country: string
    playlist_id: unsignedInteger
    group_id: unsignedInteger nullable
    relationships:
      belongsTo: Playlist, Group
