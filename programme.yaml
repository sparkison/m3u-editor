models:
  EpgProgramme:
    name: string nullable
    channel_id: string
    import_batch_no: string nullable
    data: longText
    user_id: id foreign:users
    epg_id: id foreign:epgs
    relationships:
      belongsTo: User, Epg
